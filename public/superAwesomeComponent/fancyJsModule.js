/**
 * A fancy JS module that does fanciful things
 * @param  {Number} a A number that does not get multiplied
 * @param  {Number} b A number that gets multiplied by three
 * @return {Number}   a + b  *3
 */
export default (a, b) => a + b * 3